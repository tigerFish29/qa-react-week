const Props = (props) => {
    return (
        <div>
            <h1>{props.name}</h1>
            <h2>{props.animal}</h2>
            <h3>{props.age}</h3>
            <h4>{props.breed}</h4>
        </div>
    )
}

export default Props;